/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  // Produce a smaller production image via standalone output
  output: 'standalone',
}

export default nextConfig
